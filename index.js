import chalk from "chalk";
const names = ["yahya", "teddy", "melissa", "oceane"];

function tab(array) {
for(let i =0; i<1; i++){
console.log(chalk.blue(array[0]),chalk.red(array[1]),chalk.green(array[2]),chalk.yellow(array[3]));
}

}

// console.log(tab(names));

tab(names);

